name: function
description: execute a function
tasks:
  execute:
    inputs:
      code:
        type: String
      inputs:
        type: Object
    outputs:
      result:
        data: {}
dependencies:
  function:
    image: mesg/function
